extends Node

var selected_level: String
var master_volume: float
var forward_key
var left_key
var backward_key
var right_key

# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	#campaign = $CampaignLoader.load_campaign("obama_vs_mccain")
	#$MetadataLoader.load_metadata("metadata", campaign)
